
const GameBoard = function () {
    const gameBoard = [
        {
            play: 'player1',
            move: 2,
        },
        {
            play: 'player2',
            move: 4
        }
    ]


    return {}
}
const displayController = (function () {

})()
const players = function (name) {
    const getType = () => {
        document.querySelector
    }
    getName(name)
    return { name, score }
}
document.querySelectorAll('.cell').forEach((cell, item) => {
    cell.addEventListener('click', (e) => {
        e.target.firstElementChild.textContent = 'X'
        const preMove = item;
    })
}
)
